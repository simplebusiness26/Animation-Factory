name: Autonomous Episode 001

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/autonomous-episode-001.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: autonomous-earth-needs-help-e001
  cancel-in-progress: true

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Animation Factory Episode 001 is paused by user request."
