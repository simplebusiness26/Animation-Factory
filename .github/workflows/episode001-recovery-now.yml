name: Episode 001 Recovery Now

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: earth-needs-help-e001-recovery-now
  cancel-in-progress: true

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy Episode 001 recovery is disabled."
          echo "When production is explicitly restarted, use automation/episode001_orchestrator_v6.py."
